import * as data from '../../data';
import { StaveCount } from './stavecount';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class PartSignature {
    private config;
    private log;
    private staveCount;
    constructor(config: Config, log: Logger, staveCount: StaveCount);
    parse(): data.PartSignature;
}
