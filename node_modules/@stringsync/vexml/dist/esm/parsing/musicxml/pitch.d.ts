import * as data from '../../data';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class Pitch {
    private config;
    private log;
    private step;
    private octave;
    constructor(config: Config, log: Logger, step: string, octave: number);
    getStep(): string;
    getOctave(): number;
    parse(): data.Pitch;
}
