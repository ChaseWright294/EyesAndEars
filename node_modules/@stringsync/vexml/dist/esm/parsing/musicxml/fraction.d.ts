import * as data from '../../data';
import * as util from '../../util';
export declare class Fraction {
    private fraction;
    constructor(fraction: util.Fraction);
    reify(): util.Fraction;
    parse(): data.Fraction;
}
