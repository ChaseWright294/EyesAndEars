import * as data from '../../data';
import { VoiceEntryContext } from './contexts';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class Accidental {
    private config;
    private log;
    readonly code: data.AccidentalCode;
    readonly isCautionary: boolean;
    constructor(config: Config, log: Logger, code: data.AccidentalCode, isCautionary: boolean);
    parse(voiceEntryCtx: VoiceEntryContext): data.Accidental;
}
