import { Config } from './config';
import { Logger } from './debug';
import { Score } from './elements';
export type RenderMusicXMLOptions = {
    config?: Partial<Config>;
    logger?: Logger;
};
export declare function renderMusicXML(musicXML: string | Document, div: HTMLDivElement, opts?: RenderMusicXMLOptions): Score;
export type RenderMXLOptions = {
    config?: Partial<Config>;
    logger?: Logger;
};
export declare function renderMXL(mxl: Blob, div: HTMLDivElement, opts?: RenderMXLOptions): Promise<Score>;
