import { NamedElement } from '../util';
/** The `<coda>` element is the visual indicator of a coda sign. */
export declare class Coda {
    private element;
    constructor(element: NamedElement<'coda'>);
}
