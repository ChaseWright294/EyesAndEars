import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { WedgeKey, WedgeRender } from './types';
import { RenderRegistry } from './renderregistry';
export declare class Wedge {
    private config;
    private log;
    private document;
    private key;
    private registry;
    constructor(config: Config, log: Logger, document: Document, key: WedgeKey, registry: RenderRegistry);
    render(): WedgeRender;
    private renderVexflowStaveHairpins;
    private renderSingleStaveHairpin;
}
