import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { PedalKey, PedalRender } from './types';
import { RenderRegistry } from './renderregistry';
export declare class Pedal {
    private config;
    private log;
    private document;
    private key;
    private registry;
    constructor(config: Config, log: Logger, document: Document, key: PedalKey, registry: RenderRegistry);
    render(): PedalRender;
    private renderVexflowPedalMarkings;
    private renderSinglePedalMarking;
}
