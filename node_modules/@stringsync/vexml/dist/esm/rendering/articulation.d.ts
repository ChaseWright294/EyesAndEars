import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { ArticulationKey, ArticulationRender } from './types';
export declare class Articulation {
    private config;
    private log;
    private document;
    private key;
    constructor(config: Config, log: Logger, document: Document, key: ArticulationKey);
    render(): ArticulationRender;
    private renderVexflowModifiers;
}
