import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class StaveCount {
    private config;
    private log;
    private partId;
    private value;
    constructor(config: Config, log: Logger, partId: string, value: number);
    static default(config: Config, log: Logger, partId: string): StaveCount;
    getPartId(): string;
    getValue(): number;
    isEqual(staveCount: StaveCount): boolean;
    isEquivalent(staveCount: StaveCount): boolean;
}
