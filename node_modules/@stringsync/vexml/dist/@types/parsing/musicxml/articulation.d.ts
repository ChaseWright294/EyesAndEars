import * as data from '../../data';
import * as musicxml from '../../musicxml';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class Articulation {
    private config;
    private log;
    private articulationType;
    private placement;
    private constructor();
    static create(config: Config, log: Logger, musicXML: {
        note: musicxml.Note;
    }): Articulation[];
    parse(): data.Articulation;
}
