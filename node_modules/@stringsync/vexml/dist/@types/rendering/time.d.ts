import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { StaveKey, TimeRender } from './types';
export declare class Time {
    private config;
    private log;
    private document;
    private key;
    constructor(config: Config, log: Logger, document: Document, key: StaveKey);
    render(): TimeRender;
    private getTimeSpecs;
    private toSimpleTimeSpecs;
    private toComplexTimeSpecs;
    private toFractions;
}
