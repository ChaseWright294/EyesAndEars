import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { MeasureKey, MeasureRender } from './types';
import { Point } from '../spatial';
export declare class Measure {
    private config;
    private log;
    private document;
    private key;
    private position;
    private width;
    constructor(config: Config, log: Logger, document: Document, key: MeasureKey, position: Point, width: number | null);
    render(): MeasureRender;
    private renderFragments;
    private getFragmentWidths;
}
