import { LabelFont } from './label';
export declare class TextMeasurer {
    private font;
    constructor(font: LabelFont);
    measure(text: string): {
        width: number;
        approximateHeight: number;
    };
}
