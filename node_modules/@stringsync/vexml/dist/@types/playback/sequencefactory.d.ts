import * as elements from '../elements';
import { Sequence } from './sequence';
import { Logger } from '../debug';
export declare class SequenceFactory {
    private log;
    private score;
    constructor(log: Logger, score: elements.Score);
    create(): Sequence[];
    private getSequenceEvents;
    private toSequenceEntries;
}
