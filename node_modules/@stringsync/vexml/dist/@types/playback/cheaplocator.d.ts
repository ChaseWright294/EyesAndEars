import * as playback from '../playback';
export declare class CheapLocator {
    private sequence;
    private index;
    constructor(sequence: playback.Sequence);
    setStartingIndex(index: number): this;
    locate(time: playback.Duration): number | null;
}
