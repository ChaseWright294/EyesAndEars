/** A component that covers a parent component. */
export declare class Overlay {
    private element;
    private constructor();
    static render(parent: HTMLElement): Overlay;
    getElement(): HTMLDivElement;
}
