import { ClefRender, StaveKey } from './types';
import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
export declare class Clef {
    private config;
    private log;
    private document;
    private key;
    constructor(config: Config, log: Logger, document: Document, key: StaveKey);
    render(): ClefRender;
}
