import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { VibratoKey, VibratoRender } from './types';
import { RenderRegistry } from './renderregistry';
export declare class Vibrato {
    private config;
    private log;
    private document;
    private key;
    private registry;
    constructor(config: Config, log: Logger, document: Document, key: VibratoKey, registry: RenderRegistry);
    render(): VibratoRender;
    private renderVexflowVibratoBrackets;
}
