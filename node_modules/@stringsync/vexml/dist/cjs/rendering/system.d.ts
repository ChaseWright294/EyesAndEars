import { Point } from '../spatial';
import { Config } from '../config';
import { Logger } from '../debug';
import { Document } from './document';
import { SystemKey, SystemRender } from './types';
export declare class System {
    private config;
    private log;
    private document;
    private key;
    private width;
    private position;
    constructor(config: Config, log: Logger, document: Document, key: SystemKey, width: number | null, position: Point);
    render(): SystemRender;
    private renderMeasures;
    private getMeasureWidths;
    private sliceMeasures;
    private removePartLabels;
}
