import * as data from '../../data';
import * as musicxml from '../../musicxml';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class JumpGroup {
    private config;
    private log;
    private jumps;
    constructor(config: Config, log: Logger, jumps: data.Jump[]);
    static create(config: Config, log: Logger, measureIndex: number, musicXML: {
        scorePartwise: musicxml.ScorePartwise;
    }): JumpGroup;
    getStartBarlineStyle(): data.BarlineStyle | null;
    getEndBarlineStyle(): data.BarlineStyle | null;
    parse(): data.Jump[];
}
