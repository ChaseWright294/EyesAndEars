import * as data from '../../data';
import { Metronome } from './metronome';
import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class FragmentSignature {
    private config;
    private log;
    private metronome;
    constructor(config: Config, log: Logger, metronome: Metronome);
    parse(): data.FragmentSignature;
}
