import { Config } from '../../config';
import { Logger } from '../../debug';
export declare class MultiRest {
    private config;
    private log;
    private measureCount;
    constructor(config: Config, log: Logger, measureCount: number);
    getMeasureCount(): number;
}
